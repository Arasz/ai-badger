{
  "name": "angular",
  "description": "Angular single-page application framework (TypeScript, components, RxJS, Angular CLI).",
  "detectionSignals": [
    "angular.json",
    "*.component.ts"
  ],
  "requires": ["ts", "node"],
  "commands": {
    "build": "ng build",
    "test": "ng test --watch=false",
    "lint": "ng lint",
    "run": "ng serve"
  }
}
