{
  "name": "ts",
  "description": "TypeScript language and type-system conventions layered on top of JavaScript/Node.",
  "detectionSignals": ["tsconfig.json", "*.ts", "*.tsx", "typescript in package.json devDependencies"],
  "requires": ["node"],
  "commands": {
    "build": "tsc -p .",
    "lint": "eslint ."
  }
}
