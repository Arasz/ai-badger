{
  "name": "ai-badger-pi-gates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Type-check and unit-test harness for the pi TypeScript extensions ai-badger ships. Not published, not scaffolded \u2014 it exists so `bunx tsc --noEmit -p features/pi` and `bun test features/pi` can resolve pi's own types.",
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@types/bun": "latest",
    "typebox": "1.3.7",
    "typescript": "^5.6.0"
  }
}
