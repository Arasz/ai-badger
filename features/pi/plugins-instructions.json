{
  "agent": "pi",
  "instructions": {
    "marketplace": {
      "description": "Install pi packages from npm marketplace",
      "commands": ["pi install npm:{source}"]
    },
    "tap": {
      "description": "Add pi package from git repository",
      "commands": ["pi install git:github.com:{source}"]
    }
  }
}