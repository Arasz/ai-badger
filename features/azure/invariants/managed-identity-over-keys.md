# Managed identity over shared keys

Authenticate to Azure services with managed identity and RBAC role assignments rather than connection strings, account keys, or shared access signatures wherever the target service supports it. Reserve keys for the rare case where no identity-based path exists, and never commit them.
