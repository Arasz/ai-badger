# MCP Tool Index — {{PROJECT_NAME}}
# Generated by ai-badger mcp-index init.
# Maps every MCP server tool to tags (for filtering) and intent (for semantic matching).
# Update with `mcp-index update` after adding/removing MCP servers.
#
# Tag taxonomy: see features/common/mcp-tags.json for the closed set.
#
#   Language:   csharp, typescript, javascript, python, sql, css, html
#   Action:     navigation, diagnostic, build, run, refactoring, search, read, write, terminal
#   Domain:     database, tracing, opentelemetry, browser, dotnet, semantic, files
#   Meta:       batch, slow, unsafe

version: "0.1.0"
generated_at: "{{GENERATED_AT}}"
sources:
  # Auto-populated by mcp-index init from `hermes mcp list --json`.
  # Tools marked `general` in tags need manual curation: run `mcp-index tag <tool> <tag...>`.
  # - name: rider
  #   url: http://...
  #   tools:
  #     example_tool:
  #       tags: [general]
  #       intent: "TODO: describe what this tool does"
