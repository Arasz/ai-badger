{
  "name": "react",
  "description": "React UI framework: components, hooks, Server Components/Actions where applicable.",
  "detectionSignals": ["react in package.json dependencies", "*.tsx", "*.jsx"],
  "requires": ["ts", "node"],
  "commands": {
    "test": "bun run test",
    "build": "bun run build"
  }
}
