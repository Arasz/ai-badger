# Accessibility by default

Every interactive element ships with semantic HTML, keyboard operability, and a correctly associated label/accessible name from the start — not as a follow-up pass. Treat an accessibility regression the same as a functional bug.
