{
  "lastUpdated": "2026-08-01",
  "next": "Follow-up filed from this session's research pass (not started): (a) ai-badger skills register twice in Claude Code - once bare, once ai-badger:-namespaced - because adjust_skills.py symlinks into .claude/skills/ while the plugin ships its own copies; likely the largest always-on context win available and a symlink fix, not a rewrite. (b) Move verification-evidence blocks out of owner-gate-review (~730 est. tokens) and task (~848, and they break its own platform-neutrality claim) into references/ and extensions/claude/. (c) differential-feature-refactor steps 6-7 call superpowers:brainstorming with no fallback - rewrite in the style of its own 'Relationship to a spec skill' paragraph. (d) research-with-rich-report skill: build the context-optimization half, which nothing occupies; the structured-report half is already done five times over. Open backlog: #243 (removed catalog skill leaves its scaffolded directory - PR in flight from a dispatched agent, merge when green) and #232 (a hook reached from a child interpreter can spawn a daemon the test suite cannot see - residual, filed with evidence). Follow-up from the 0.60.0 review, low severity: the dispatch gate's lane lookup walks past the git root (F5); Copilot/Junie docs carry no delegation-map pointer by decision.",
  "completedTasks": [
    {
      "id": "documentation-skills",
      "title": "Skill catalog documentation + ai-badger skills intro article",
      "summary": "Five documents listed the shipped skills and all five were stale, each missing owner-gate-review, commit-reminder and differential-feature-refactor (framework-architecture.md listed only 7 of 13). Added docs/skills.md as the single catalog - purpose, on-disk effect and trigger per skill, grouped by job, with the first screenshot in the docs tree (docs/screenshots/owner-gate-review.png); the other four lists now point at it. Count reconciled against badger_lib.SKILL_SCOPES and index.json: 12 common + auto-wm stack-local, no discrepancy. Documentation only - release_guard confirms no shipped-surface change since v0.61.0, so no VERSION bump and no changelog entry. Separately in arasz-home-page (PR #189): added an IMAGE block to the article pipeline (parser/serializer/model/renderer, 23 new tests) since it had no way to show a picture, and wrote the skills-tour article at id 7, verified rendering in a browser.",
      "pr": 249,
      "date": "2026-08-01"
    },
    {
      "id": "full-delegator-persona",
      "title": "Delegation becomes a mechanism: persona lanes, dispatch gate, delegator (0.60.0)",
      "summary": "Shipped ADR-0015's three levers: model: lanes in persona frontmatter (CLAUDE_KEYS passes it through, Copilot drops it, catch-all test), a PreToolUse dispatch gate denying model-less dispatches with no lane, and the delegator persona plus a scaffold-generated .ai-badger/delegation.md. Hook commands now survive a missing script (worktree ${CLAUDE_PROJECT_DIR} resolves to the main checkout — found when the gate blocked this session's own dispatch) and a changed command shape supersedes the old registration. Also merged: #241 banner-below-frontmatter fix (0.59.1, PR #244). Tags 0.59.1 + 0.60.0 pushed.",
      "version": "0.60.0",
      "pr": 245,
      "date": "2026-07-31"
    },
    {
      "id": "badger-framework-mascot-and-logo",
      "title": "Badger mascot + logo: four propositions, D chosen as the mark",
      "summary": "The project had no mark. Drew four hand-authored SVG propositions (A circuit-striped head on slate; B amber terminal mascot peeking over a window; C flat 16px-legible glyph with a neural chain; D a cross of A and B on one teal palette). D was chosen and promoted to docs/brand/logo.svg as the README hero; A, B and C were deleted and survive only in the history of PR #125. Hand-drawn rather than photo-traced so no third-party image licence attaches to the primary mark; each was rasterised with qlmanage and reviewed by eye at 420px and 16px while drawing. docs/brand/README.md carries the palette spec, clear-space and usage rules, and the variants still undrawn (wordmark lockup with outlined letterforms, monochrome, a simplified head-only mark for favicon sizes, PNG exports). No VERSION bump or changelog entry: gates/release_guard.py reported no shipped-surface change and CONTRIBUTING.md defers that call to it, so the 'always bump VERSION' invariant does not fire for a docs/assets-only change.",
      "date": "2026-07-29",
      "pr": "https://github.com/Arasz/ai-badger/pull/125"
    },
    {
      "id": "clean-up-docs",
      "title": "Trim docs/ to product documentation; move plans/reviews/designs/specs to .tmp (0.39.1)",
      "summary": "docs/ had become two trees sharing a directory - one documenting ai-badger, one documenting the building of it. 32 of the 51 non-changelog documents were the second kind (plans, session checkpoints, waves, reviews, audits, incident post-mortems, research passes, superseded designs and specs); those 32 plus the 3 files under .hermes/plans/ moved to a newly gitignored .tmp/, leaving 19: 11 ADRs and their index, plus 7 references. Selection test was 'would a reader of the product documentation lose something true', and every candidate was re-verified against the code at 0.39.0 before being carried forward - anything unverifiable was dropped rather than copied. Knowledge folded forward: framework-architecture.md gained scaffold.py's real shape (one Scaffolder holding a ScaffoldContext plus six collaborators) and the fact that proxy files were dropped, not deferred; hermes-claude-compatibility.md gained the learned-skill sync contract (the largest loss the move would otherwise have caused - a shipped feature that writes into a consumer's repo, described nowhere reachable); authoring-a-feature.md gained the mcp-servers.json contract it had only forward-referenced; scripts.md gained call-behaviorist. Reading thirty documents against the code caught two statements that had stopped being true: RELEASING.md claimed the untagged-release check was informational (0.35.3 made it return 1) and SECURITY.md miscounted unsafe_literals.py fixture matches at three (one remains). 22 docstrings and comments across features/, skills/ and tests/ stopped citing documents that no longer open. DELIBERATELY NOT DONE: the ADR and changelog links to the moved files were not repaired - an ADR is append-only under ADR-0001 and editing a past changelog falsifies what that release said, so repairing them would mean falsifying a record to satisfy a link checker; 9 prefixes went into .docs-guard-ignore instead, which is what that file is for. COMPLICATION: partway through, another session merged two unrelated streams into task/clean-up-docs - the #110 drift fix (drift compares the framework, not the project against itself) and an 'every check must prove it can fail' testing stream adding tests/test_every_check_can_fail.py - and cut release 0.40.0 on the branch, so PR #111 squash-merged 0.39.1 and 0.40.0 together as 147f98e, wider than its title. Audited 2026-07-29 before closing the record: the squash tree is byte-identical to the branch tip, all 35 deletions and 3 additions survive on main at 0.45.0, and the gates pass (docs_guard 109 documents, index.json up to date, 1624 tests passing).",
      "date": "2026-07-28",
      "pr": "https://github.com/Arasz/ai-badger/pull/111"
    },
    {
      "id": "hook-design",
      "title": "Commit-reminder hook: advisory PostToolUse nudge for all three agents (0.35.0-0.35.2)",
      "summary": "Built a common/ hook that watches live `git status --porcelain` uncommitted-file count after edit-shaped tool calls and nudges (never blocks/denies/gates - additionalContext only, honoring the 0.33.0 no-third-party-tool-call-interception precedent) once a threshold (default 5, AI_BADGER_COMMIT_REMINDER_THRESHOLD) crosses, with a ratchet-debounce that re-arms after every commit instead of latching forever (avoids the 'adjustments reported as drifted forever' bug class). Impact reason is pluggable: a pure file-count default (features/common/skills/commit-reminder/scripts/impact_estimator.py), extended by an opt-in (AI_BADGER_COMMIT_REMINDER_IMPACT=graph) code-review-graph CLI subprocess call for real blast-radius data, guarded so any failure/absence falls back silently and the ~15-20s call only runs once the cheap check already decided to fire. Shipped for all three agents from one shared commit_reminder.py/impact_estimator.py core: Claude+Copilot via the existing hooks-manifest.json/hooks.json PostToolUse wiring (fixed Copilot's adjust_hooks.py to also carry the matcher field through, previously dropped), Hermes via post_tool_observer stashing a pending reminder that pre_llm_inject_context surfaces on the next turn. Mid-task user feedback (f: marker) required instrumenting with the existing debug_log/call-behaviorist audit facility rather than shipping silently, under component name commit_reminder_hook; a follow-on continuation (0.35.1) generalized that same vendored-debug_log pattern to the 5 other framework hooks that had never called it. PR #105; 1366 tests passing at 0.35.2.",
      "completedAt": "2026-07-28T00:50:00Z"
    },
    {
      "id": "full-project-code-review",
      "title": "Full-project multi-agent code review + Wave 1 remediation (0.19.0)",
      "summary": "Ran 8 parallel reviewers (architecture, security, python, ai-agent design, skill engineering, tests, silent failures, docs) over the whole project, then an Opus integrating pass that VERIFIED every Critical against the code rather than trusting it: 23 raw -> 19 distinct -> 7 confirmed, 12 downgraded to Important, 5 sub-claims rejected (incl. security's 'live RCE', correctly latent, and docs' 'nothing shipped since v0.2.0', disproved by plugin.json). Root cause behind four dead features: tests validate synthetic fixtures instead of the real catalog, so install_skills emitting zero install commands and mcp_index_hook targeting a nonexistent script both shipped green. Wave 1 fixed the five destructive Criticals TDD-first: sync_plugin_skills --dry-run rmtree'd before its own guard (wiped all 8 shipped skill dirs); five sites treated an unparseable config as {} then overwrote it, two of them machine-global (~/.claude/settings.json, ~/.hermes/config.yaml); a user-scope Hermes hook exec'd a cwd-derived path the framework never creates (removed, NOT renamed - renaming to the real mcp_index.py would have armed it; guard test parametrized over both names); install_cron could replace the whole crontab when crontab -l failed, and ran by default (now opt-in --cron); the learned-skill secret scanner skipped symlinks while copytree dereferenced them, leaking outside-root file contents into an un-gitignored dir. Also aligned pre-commit's pylint with pyproject on E0401 - the two gates disagreed, arbitrarily failing any small new test file. 691 -> 727 tests. Waves 2-5 planned but deliberately unshipped; hard constraint recorded: the auto-wm denylist (F-12) MUST land before session_start_hook is wired (F-07), or the framework ships a machine that can enable unattended auto-approval of its own tool calls. PR #78.",
      "completedAt": "2026-07-27T00:25:00Z"
    },
    {
      "id": "hermes-skills-sync",
      "title": "Hermes learned-skills sync (#67) + reverse #58 + plugin-hook cwd fix (#76)",
      "summary": "Reviewed docs/research/hermes-learned-skills-sync.md and corrected four load-bearing claims: external_dirs was already tried and reverted (#58/v0.7.1); the ~/.hermes/skills/ symlinks still exist on disk so a naive scan re-imports framework skills; NOTHING distributed .ai-badger/skills/ to any agent; and the hook mechanism is the Python plugin this repo already ships, not ~/.hermes/hooks/. Wrote a staged TDD plan with acceptance criteria, then implemented it: learned_skills_sync.py (5 gates, learned.json provenance, opt-in --reconcile, unsafe-literal refusal), feed-badger per-skill grouping, post_tool_call wiring + ~/.hermes/plugins install, and the #58 reversal restoring namespaced symlinks without the original rmtree data-loss hazard. Key discovery beyond scope: Hermes passes NO cwd to any plugin hook, and names the prompt kwarg user_message not message — so the drift notice, the pre_llm drift line, and the MCP index logging had never fired (#76, fixed). CodeQL's clear-text-logging alert was name-heuristic, not flow-based: Copilot autofix traded alert 13 for 14 and cost CLI output, so it was reverted and the identifiers renamed to unsafe-literal terms, which cleared it. PRs #73/#74/#75 + recovery #77; 683 tests green at 0.18.0.",
      "completedAt": "2026-07-26T21:20:00Z"
    },
    {
      "id": "den-refresh-recovery",
      "title": "Error recovery + GH issue creation for scaffold skills",
      "summary": "Added Error Recovery section to den-refresh, welcome-ai-badger, feed-badger. Agent parses errors, tries automatic fixes (config validation, version sync, index rebuild, deps install, gh auth), and offers to create GH issue in Arasz/ai-badger with user permission. PR #51.",
      "completedAt": "2026-07-26T15:05:00Z"
    },
    {
      "id": "split-python-files",
      "title": "Split Python files into smaller domain-specific modules",
      "summary": "Refactored scaffold.py (1455 lines) into 6 domain modules via mixin pattern: mcp_tools.py, hook_wiring.py, template_rendering.py, agent_files.py, extensions.py. scaffold.py reduced to ~600 lines. All 596 tests pass. PR #50.",
      "completedAt": "2026-07-26T13:00:00Z"
    },
    {
      "id": "mcp-servers-feature",
      "title": "MCP server declarations per stack feature",
      "summary": "Added mcp-servers.json per stack for declaring recommended MCP servers. Scaffold collects, merges (externalTools > stack > common), and writes to agent-specific configs: .mcp.json (Claude/Hermes/Junie), ~/.hermes/config.yaml (Hermes user-scoped), ~/.claude/settings.json (Claude user-scoped), .github/copilot/mcp-config.json (Copilot). 48 new tests, 590 total passing.",
      "completedAt": "2026-07-26T09:25:37.954922+00:00"
    },
    {
      "id": "den-stack-detection",
      "title": "detect new stacks after initial scaffold",
      "summary": "Added detect_new_stacks() to drift.py. Wired into den-refresh as newStacks report field. 4 tests. Fixes gap where new stacks (e.g. hermes) were invisible to den-refresh.",
      "completedAt": "2026-07-26T11:00:00Z"
    },
    {
      "id": "crg-integration",
      "title": "code-review-graph integration into ai-badger",
      "summary": "Added externalTools config for MCP tool injection. Integrated code-review-graph (MIT, 30 tools). TDD with 8 tests. PR #48 merged.",
      "completedAt": "2026-07-26T10:30:00Z"
    },
    {
      "id": "dir-hash-drift",
      "title": "dir-hash-drift detection",
      "summary": "Completed 2026-07-24.",
      "completedAt": "2026-07-24T08:28:47Z"
    }
  ]
}
