{
  "lastUpdated": "2026-08-31",
  "next": "(1) IMPLEMENT the default-projectId-from-cwd resolver in ~/RiderProjects/ai-raccoon - design committed there (docs/plans/default-projectid-from-cwd.md, 2cda253b); seam is ToolGate.RequireAsync, needs dotnet-engineer lane + tests; (2) after the resolver ships: pi sessions stop passing projectId explicitly. Also open: the pbi session's repo-B surgery sits rebased on 0.148.0 at task/pbi-minimal-pi-layer @ 1a8b209d in the ai-badger checkout (manifest conflict resolved by regeneration; bun features/pi 62/0, grep residue zero, index verified; FULL pytest was aborted mid-run - witness it, then ff main and push). One-off repo-A bun flake (2 fails, 3 consecutive green after) unattributed; mirrored saveDisabledTools atomicity tests skip in repo A under the mock-capture guard (fork suite covers them live).",
  "completedTasks": [
    {
      "taskId": "aib-scaffold-freshness-guard-blindspot-proof",
      "title": "Scaffold freshness guard must be blind-spot-proof (F2 + --skills '' trap)",
      "finished": "2026-08-31",
      "version": "0.150.0",
      "pr": "https://github.com/Arasz/ai-badger/pull/456",
      "summary": "Root cause found and verified: NOT nondeterminism — the scaffolder's --skills argv accepted a literal two-char '' (non-shell transport) as a garbage skill name, silently skipped it, and delivered only include-derived + stack-local skills (11/32, 128 entries, exit 0); the guard's --skills '' recovery inherited any narrowing, so lost mirrors were never regenerated and tree-vs-tree diffs saw nothing (F2). A narrowed manifest had shipped in-repo at ea17ae60 (0.140.0). Fixes: D3 argv validation (quoting artifacts always refused exit 2; unknown names refused unless catalog-or-manifest-known; ''/',' recovery untouched); D1 shared expected_skill_names oracle in badger_lib (delivery BLOCK order — flat-sorted empirically fails healthy trees via manifest row order; Scaffolder consumes only the include-derived block so narrow argv never gains defaults); D2 guard fail-fast on manifest⊊expected (Narrowing→exit 1, names mirror PATHS, prints remediation block, hedges index-staleness); R3 guard argv carries the explicit config-derived set (never --skills '', never bare omission — the front-door trap when DEFAULT_SKILLS walks empty); R4 AC0 double-scaffold confirmed 42-path loss already repaired at 0.149.1; R5 gotcha in welcome-ai-badger SKILL.md + guard rationale clause. Release 0.150.0, tag on remote, CI green (tests/lint/gates/consumer-journey). MoE: 3 plan lanes (architect lane died on upstream timeouts twice — findings transcribed from its probe log; test-engineer + reviewer re-dispatched with incremental-write protocol), api-engineer APPROVE-WITH-CHANGES + qa REQUEST-CHANGES (probes falsified 3 RED expectations; recipes rebuilt) all folded. Red-first: AC1–AC4 witnessed RED pre-fix, GREEN post-fix. Suite 4763/0 twice consecutively (d-16 ruling satisfied); F1 in-suite flake not reproduced post-rewrite (3 negative runs, root cause still unknown — time-box closed). Live validation: the main checkout was hit by the defect's exact signature (11:38 artifact, actor unidentified, not this session's lanes) — healed via ff + link restore, guard PASS on the healed tree. POST-MERGE: qa review-tests lane dispatched (d-51), verdict pending at task close.",
      "hasNotes": true
    },
    {
      "taskId": "aib-delegation-usage-parser-migration-note",
      "title": "Cross-repo follow-ups: delegation_usage parser + task-skill receipts note (0.149.0, PR #455)",
      "finishedAt": "2026-08-30T23:25:33Z",
      "summary": "pi_session_source.delegation_usage implemented — parses ~/.pi/agent/subagent-logs/<runId>.jsonl (R4 frozen contract) into the hermes-shaped record (Σ input+output, first model, apiCalls, at-chain exit→turn→start); settled = exit OR agent_settled (M1: TUI-aborted runs carry real spend, witnessed by the field-for-field d-1 fixture); traversal guard + tolerant parsing; task_tracker subagent --delegation works under pi. Task-skill receipts migration note in SKILL.md/brief/delegator persona (receipt default, delegation-result followUps, details.usage input+output cache-excluded, background:false, --delegation <id>) — held under the skills_lint cap by net-compact rewrite. 0.149.0 (minor per convention; plan's 0.148.1 amended). Reviewer d-16: merge-after-folds, F3–F7 folded, F1 witnessed twice (4714/0 ×2).",
      "decisions": [
        "settled marker = exit line OR agent_settled line — settleAborted writes no exit line, so exit-only refuses exactly the aborted-spent runs cost accounting exists for (deviation from hermes not-completed→None, documented in parser docstring)",
        "totalTokens = input+output only, cache excluded — hermes parity keeps subagentTokens comparable across sources; receipts (details.usage) are authoritative over log parses (log tee elides middles)",
        "skill mirrors must be regenerated with the scaffolder's DEFAULT --skills, never the guard's --skills '' remediation — the empty list narrows manifest's managed set and makes the freshness guard green-on-broken (filed as framework defect F2)"
      ]
    },
    {
      "taskId": "aib-pi-stack-mcp-skills-parity",
      "title": "pi stack parity: project-scoped MCP config + skills discovery (0.148.0)",
      "finished": "2026-08-30",
      "version": "0.148.0",
      "pr": "https://github.com/Arasz/ai-badger/pull/454",
      "summary": "pi discovery is now project-scaffold-defined, claude-parity by construction. Fork (Arasz/pi-mcp-tools @ 8da005e + README 377eb0f): claude-format converter (stdio/absent->local, HOME-prefix expansion, other ${VAR} skip, tools-star->no-filter - the pass-through would have thrown RegExp star per tool and killed every scaffolded server; poison-regex fail-open per-pattern; http/sse->remote), config load moved to session_start under ctx.isProjectTrusted(), merge ledger reported by the mcp_list_servers TOOL (extension commands throw when queued headless - the only headless channel), atomic saveDisabledTools, stale entry points fixed. Adapter: ungated resources_discover contributing cwd/.ai-badger/skills (vendored byte-identical, publish --check clean). Adjustments repurposed migration-only: shape-aware removal (regenerate-from-declaration deep-equal, shlex-or-literal command tolerance for the c7d0d528 drift), per-extension capability-marker gates (.ai-badger-capability-project-scope-mcp / -resources-discover - never version numbers). ADR-0023; support.json pi rows pinned by test_support_json_honesty.py; precondition pin keeps minimal-project-headless-arming true by construction. OWNER GATE 5/5 APPROVE (D1 isProjectTrusted-only with measured semantics; D2 skills ungated; D3 shape-aware+marker-gated migration; D4 mcpDisabledTools global + atomic drive-by; D5 remote mapping). TWO PLAN PREMISES FALSIFIED EN ROUTE: (1) 'headless arms nothing until /trust' - pi short-circuits trust to true for projects without trust-requiring resources (.mcp.json/.ai-badger/.pi-agents are not on the list), so scaffolded projects arm in ALL modes day one; the gate bites only when .pi/settings.json appears (ledger untrusted-project rows make it visible); (2) the 'latent /new bug' - pi re-invokes the extension factory per session replacement; per-session Maps made the duplicate-registration guard unnecessary. LIVE CUTOVER on this machine: fallback arming, migration (global mcp+skills removed, user keys byte-clean, emptied key dropped), minimal-project headless arming = exactly the declared five, fragile-case flip both directions, cross-project isolation + rider remote (ai-raccoon, job-search-ai-assistant), skills two-sided probe, marker-gate both directions, idempotence byte-diff empty; stale ~/.pi/agent/extensions/ai-badger-subagent/ orphan removed (documented pbi cleanup - it double-registered delegate and broke extension loading in .pi/agents projects). Gates: pytest 4707/0, bun 404/0 (3x; one unattributed 2-fail flake never reproduced), fork vitest 131, CI all green. /new interactive lifecycle check = owner step. Delegation flakiness (3 upstream idle timeouts + 1 abort) forced main-loop execution of P2/P4/P5 per user instruction - reduced review rigor noted; implementation review + QA done in-session."
    },
    {
      "id": "hermes-isolation-gap",
      "title": "Hermes: honest dispatch-gate exemption + surface delegation.worktree_isolation",
      "summary": "Shipped via PR #440 (merged 2026-08-28); .ai-badger/hooks/hermes_isolation.py identical on main. Branch closed as stale (later commits were rebase/scaffold artifacts).",
      "completedAt": "2026-08-30T16:14:29Z"
    },
    {
      "id": "incident-guard",
      "title": "Guard the git dir against hand edits (.git/config truncation incident)",
      "summary": "Shipped: guard + health hooks + invariant on main, review fixes via PR #445 (merged 2026-08-28). Uncommitted worktree test was redundant with main's test_h3a/test_h3b; discarded. Lane branches a/b/d/h closed as integrated.",
      "completedAt": "2026-08-30T16:14:29Z"
    },
    {
      "taskId": "aib-interrupt-grade-status-report-skill",
      "title": "status-report skill: interrupt-grade mid-task status snapshot, default scope",
      "finished": "2026-08-30",
      "summary": "New default-scope common skill status-report: answers 'where are we?' mid-task in one cheap read — current task (latest IN_PROGRESS wins, other open visible), progress checklist (plan packages + checkbox counts, newest-file-fallback marked), next (state.json verbatim), delegation (recorded subagents, lane worktrees for open taskIds only, live sessions). stdlib-only script, exit-0-everywhere, per-section degradation. Important-by-default contract: never deferred to task end, never delegated. Review round 1 fixed lane prefix over-match (-lane- only), stat() race, fallback-marker pin. DISCOVERED: skill delivery resolves via index.json — index BEFORE self-scaffold or the new skill silently skips delivery (guard cannot see it, --skills '' reuses manifest). Also: v0.146.0 had never been tagged (squash #450 carried 4 unpushed local commits); tagged f1305bd9 and pushed. Release 0.147.0 (squash 77dc19bf, tag d7ba6a59), CI green."
    },
    {
      "taskId": "aib-prompt-markers-importance-token-grammar",
      "title": "prompt-markers: ! importance token on every marker (framework grammar)",
      "finished": "2026-08-29",
      "summary": "Framework grammar for the marker importance token (pi side shipped in the integration repo). match_marker builds each catalog prefix's !-variant and returns (marker, prefix, bang); bang injects injectInterrupt (important marker) or inject + IMPORTANCE_SUFFIX. Catalog: important splits into i:/important: base + i!:/important!: interrupt. Templates, agent standing lists and SKILL.md updated. DISCOVERED: markers-context.json is seed-once (#15) and this repo's deployed copy had frozen at three markers — q:/i!: never fired through the local hook; re-seeded. Also: standing-list test excludes .ai-badger.bckp/ snapshots; 0.145.0 tag applied retroactively. Release 0.146.0 (f1305bd9), full suite 4674 passed."
    },
    {
      "taskId": "aib-pi-marker-importance-delegation-status",
      "title": "pi extension: marker ! importance (mid-turn interrupt) + delegation working status",
      "finished": "2026-08-29",
      "summary": "pi-badger-integration @ 81ead09: extensions/session-signals.ts — marker grammar split (meaning + importance token !; every marker can interrupt: f!:, q!:, hint!:_; legacy i!:/important!: unchanged) and ctx.abort() from pi's input event when streamingBehavior is set (the ONLY mid-turn interrupt path on pi); delegation working status (footer via ctx.ui.setStatus, elapsed timer, cleared on tool_result; PI_BADGER_DELEGATION_TOOLS override). 29 new tests (104 total), tsc clean, published to user scope, live load verified. FOLLOW-UP (not done): ai-badger framework-side marker grammar — markers-context.json prefixes need !-tolerant parsing + templates standing list + user_prompt_hook.py + tests; marker! interrupt semantics for Claude/Hermes hosts remain advisory-only (no mid-turn hook exists there)."
    }
  ]
}