# Governance

Prose only — no rule headings, so this file contributes zero parsed rules. It exists so the
fixture has a real, citable, non-rule file under `references/` (MoE-2-ruleset.md §9), matching
the shape of the real ruleset where `references/governance.md` documents §9's addition and
retirement bars without itself being a rule.
