# Principles (L0)

Invariant-style statements, not falsifiable rules — no `severity`/`evidence`/`flag`/`check`, per
MoE-2-ruleset.md §1.4. Kept out of both generated walks (no `meta:` line applies).

**`T0-01` — A check you have not seen fail is not a check.**
- *cites:* invariant `prove-the-check-fails`
