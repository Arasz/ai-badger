# 0.172.10 — dependency bumps: mutmut 3.8.0, codeql-action 4.38.0, semantica 0.6.8

**Patch** (dependency floors and action pins only, no behavior change) · 2026-09-24

## What changed

Merges four Dependabot bumps into one commit:

- `requirements-mutation.txt`: `mutmut>=3.7.0,<4` → `>=3.8.0,<4`.
- `.github/workflows/codeql.yml`: `github/codeql-action/init` and
  `github/codeql-action/analyze` `5595ccaf…` (v4.37.6) → `b96794f015dfd88f77b49b1c93e0fa7110f94c63`
  (v4.38.0). Both jobs stay pinned to the same release SHA.
- `engine/requirements.txt`: `semantica>=0.6.7` → `>=0.6.8`.

The `.ai-badger/engine/` scaffold mirror is regenerated in the same release.

## Verification

- `mutmut==3.8.0` and `semantica==0.6.8` each install cleanly in a scratch venv.
- `gates/workflow_lint.py` PASS — every `uses:` still a 40-character commit SHA.
- Touched-surface suites green: `test_conftest_load_script.py`,
  `test_deps_guard.py`, `test_dependency_honesty.py`, and the semantica MCP
  script/export/skill tests (154 passed).
