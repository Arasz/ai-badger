# 0.11.1 — MCP index text parsing + tool indexing

## Fixed

- **mcp-index text parsing fallback** — `_fetch_mcp_tools()` now falls back to parsing
  `hermes mcp list` text output when `--json` flag is unavailable. Previously the script
  crashed with a hard error.
- Applied to both source template (`features/`) and scaffolded copy (`.ai-badger/`).

## Added

- **98 MCP tools indexed** across 9 servers in `.ai-badger/mcp-tools.yaml`.
- **Hermes MCP serve** added to `.mcp.json` so Claude Code and other agents can use
  Hermes conversations as an MCP tool.
- 3 new tests for text parsing fallback and `--from-json` init.
