# 0.10.2 — known-gaps cleanup

## What changed

- **Non-standard agent file detection.** `scaffold.py` now warns when a non-standard
  agent file equivalent exists (e.g. root `COPILOT_INSTRUCTIONS.md` alongside the scaffolded
  `.github/copilot-instructions.md`). Detection uses `_NONSTANDARD_AGENT_FILES` mapping —
  extensible for future agents.

- **Task lifecycle integration test.** Added `test_full_lifecycle_start_subagent_finish_grade`
  exercising the complete start → subagent → finish → grade → status cycle end-to-end.

- **Stale doc references fixed.** Updated `ai-badger-framework-design.md`,
  `framework-architecture.md`, `authoring-a-feature.md`, and `hermes-claude-compatibility.md`
  to reflect the inline extension layout from v0.10.1.

- **Known-gaps audit.** Closed gaps #8 (non-standard detection), #9 (lifecycle test),
  #10 (advisory plugin install — already resolved by `--execute` flag). Only #11 (MVP catalog)
  remains open.

## Tests

514 passed (was 511 in v0.10.1). Lint 10.00/10.
