# 0.116.2

Fix: install follow_through.py in hermes plugin. The passive follow-through hook (stash search results, match read_file within 60s) was referenced by ai_badger_hooks.py but the file was not in the copy list — the lazy loader silently returned None. Added to USER_PLUGINS so scaffold installs it.
