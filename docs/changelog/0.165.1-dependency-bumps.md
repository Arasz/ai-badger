# 0.165.1 — dependency bumps: pylint 4.0.8, semantica 0.6.7

**Patch** (dependency floors only, no behavior change) · 2026-09-08

## What changed

Merges two Dependabot bumps into one commit:

- `engine/requirements-dev.txt`: `pylint>=4.0.6` → `>=4.0.8`.
  The `astroid<4.1` ceiling is unchanged — the installed pair
  (pylint 4.0.8 + astroid 4.0.4) stays under it.
- `engine/requirements.txt`: `semantica>=0.6.6` → `>=0.6.7`
  (pip resolves 0.6.8, which satisfies the new floor).

The `.ai-badger/engine/` scaffold mirrors are regenerated in the same commit.

## Verification

- `pip install --dry-run -r engine/requirements.txt -r engine/requirements-dev.txt` resolves.
- Touched-surface suites green: `test_deps_guard.py` +
  `test_dependency_honesty.py` (24 passed).
