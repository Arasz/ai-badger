# 0.168.0 — high tier prefers deepseek-v4.1-flash

**Minor** (scaffold-affecting) · 2026-09-11

The `high` group's preferred pin is now `openrouter/deepseek/deepseek-v4.1-flash`
($0.15 in / $0.60 out per M). It displaces `openrouter/meta/muse-spark-1.3-contributor`,
which stays visible as high's demoted tail (`status: "demoted"`, `revisionWatch: true`):
it remains medium's preferred pin and the contributor half of the
`muse-spark-1.3-weights` twin. `registryVersion` stays 1 — no member was added or
removed and no price moved; the preferred index rotated.

Price evidence: pi's models-store and the OpenRouter models API agree on the
deepseek-v4.1-flash rate (checked 2026-09-11). Low and medium pins, and the demoted
tail's stored price, carry the 2026-09-05 measurement.

## Advisory spend bands (measured 2026-09-11 — stale the moment prices move)

Reference task unit: 100K input + 20K output tokens. Bands run from the tier's
preferred pin to its dearest *deciding* pin; the demoted tail is display-only and
outside every band. The registry is the truth; this table is not.

| tier | preferred → dearest deciding | advisory ref-unit spend |
|---|---|---|
| low | glm-5.3-flash → haiku-4.5 | $0.0125 → $0.20 |
| medium | spark-contributor → terra | $0.014 → $0.44 |
| high | deepseek-v4.1-flash → fable-5.1 | $0.027 → $2.00 |

## Upgrade notes

- A persona `level: high` picks up the new pin on the next refresh
  (`welcome-ai-badger` / `den-refresh`); an explicit `model:` still wins verbatim.
