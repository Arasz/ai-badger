# 0.172.12 — codeql-action 4.38.1

**Patch** (action pin bump only, no behavior change) · 2026-09-24

## What changed

Applies Dependabot #489 and #490 in one commit:

- `.github/workflows/codeql.yml`: `github/codeql-action/init` and
  `github/codeql-action/analyze` `b96794f015dfd88f77b49b1c93e0fa7110f94c63` (v4.38.0) →
  `1c5b675653bb5c22dbe9b12b556ec555138e09fd` (v4.38.1). Both jobs stay pinned to the
  same release SHA.

## Verification

- `gates/workflow_lint.py` PASS — every `uses:` still a 40-character commit SHA.
- `tooling/validate.py --all`, `version_sync.py --check`, `changelog_index.py --check`,
  `index_build.py --check`, `gates/release_guard.py`, `scaffold_freshness_guard.py` and
  `sync_plugin_skills.py --check` all PASS.
