# 0.135.3 — Copilot round-2 review fixes for #431

**Patch** — correctness fixes from the second Copilot review of the prompt-rules phase-2 hooks.

## What changed

- **task skill**: the one-turn-specification rule again names data sources and requires the
  final ask last — both parts were dropped by the earlier 5000-char trim.
- **reasoning_scaffold_lint**: negation detection is now scoped to the clause governing the
  matched phrase. `Do not skip validation; think step by step` is correctly reported, while
  `no "think step by step"` policy lines remain suppressed.
- **grounded_feedback**: `pop_pending_feedback` / `pop_pending_reminder` are fail-open — a
  persistence failure while clearing the stash no longer escapes into
  `pre_llm_inject_context`; the message still returns and a stale entry may re-surface.

## Upgrade notes

No configuration changes required.
