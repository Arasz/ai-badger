# 0.7.2 — move auto-wm to the Claude stack

**Date:** 2026-07-24

## Changed

- **auto-wm skill** moved from `features/common/skills/` to `features/claude/skills/`. This skill uses Claude Code's `PreToolUse` hooks which are Claude-specific — it was incorrectly placed in common.
