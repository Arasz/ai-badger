# 0.116.5 — Code-Review-Graph MCP Scripts & Smart .venv Stack Detection

**Type:** MINOR
**Date:** 2026-08-12

## What Changed

### MCP Server Catalog (`code-review-graph`)
- Replaced manual skill setup notes for `code-review-graph` with executable prerequisite check (`check.py`) and full venv installation scripts (`install.py`) inside `features/common/mcp/code-review-graph/scripts/`.
- Updated `features/common/mcp/code-review-graph/meta.json` to point `prerequisite.check` and `prerequisite.install` to the new Python scripts.
- Removed manual skill setup notes at `features/common/skills/worktree-agent-isolation/references/code-review-graph-setup.md` and cleaned up references in `SKILL.md` and `comparative-agent-experiments.md`.

### Smart `.venv` Stack Detection (`welcome-ai-badger`)
- Enhanced `detect.py` stack detection so a `.venv` directory containing only catalog MCP tool server packages (e.g. `code-review-graph`, `semantica`) does not falsely trigger `python` stack detection in non-Python projects when no user Python project manifest exists.
