# Seeded secret — acceptance evidence for W4.a, delete this branch

An invented AWS access key **ID** (not a secret key, not a real account) so `gitleaks` has
something to find. This branch exists only to prove the required check blocks, and is deleted
as soon as the check reports.

    aws_access_key_id = AKIA4RJ7QW2MZX6VTLDP
