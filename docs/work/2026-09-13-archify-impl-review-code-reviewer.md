# Implementation review — archify diagram skill default integration

- Task: `aib-archify-diagram-skill-default-integration`, v0.171.0, branch `c757b858`, diff base `f9c6f287`.
- Scope: the 296-file diff is dominated by the vendored packet; everything ai-badger authored was read line by line against `docs/work/2026-09-13-archify-diagram-skill-plan.md` and ADR-0030.
- Method: downloaded the genuine v2.16.0 release asset to `/tmp` (sha matches the pin), re-vendored it into a scratch root and `diff -r`ed against the committed tree; adversarial zips; a mounted disk image as `TMPDIR` to force a cross-device rename; focused pytest/node/pylint/gate runs via `/Users/arasz/RiderProjects/ai-badger/.venv/bin/python3` (the worktree has no `.venv`).
- Environment: Python 3.14.7, pytest 9.1.1, node v26.8.2.

## Findings

| ID | Sev | Claim (location) | Evidence (command → output) | Minimal fix |
|---|---|---|---|---|
| F1 | SHOULD | `--revendor` stages in the system temp dir and `os.replace`s the tree across filesystems; on any host whose tmp is a different mount (Linux tmpfs `/tmp`) the documented re-vendor cannot complete. `tooling/vendor_archify.py:399` (`TemporaryDirectory`), `:445` (`replace_tree`) | `TMPDIR=/Volumes/aibtmp … vendor_archify.py --revendor /tmp/aib_exdev/synth.zip --expect-sha256 ac31d114… --root /tmp/aib_exdev/fw` → `rc=2 could not replace …: [Errno 18] Cross-device link: '/Volumes/aibtmp/archify-revendor-*/staged' -> '/private/tmp/aib_exdev/fw/…'`; identical command with default `TMPDIR` → `rc=0` | Create the staging dir under `target.parent` (`target.parent.mkdir(parents=True, exist_ok=True)` before `TemporaryDirectory(…, dir=target.parent)`), or catch `OSError` and fall back to `shutil.move`. Fail-closed today, but the origin op is unusable on common Linux hosts |
| F2 | SHOULD | `target_is_dirty` runs the first `git status` from the caller's CWD, not the target's repo: `--root DIR` pointed at another framework root refuses a *clean* target. `tooling/vendor_archify.py:353` | Clean, committed target, tool run from the worktree: `… --revendor synth.zip --expect-sha256 ac31d114… --root /tmp/aib_exdev/fw2` → `rc=2 cannot assess target cleanliness: fatal: … is outside repository at '<worktree>'`. Same tool run with `cwd` = target repo and a dirty file → `rc=2 refusing: … has uncommitted changes — commit or stash first` | Pass `cwd=str(target if target.is_dir() else target.parent)` to the first `subprocess.run` (the fallback probe already does this) |
| F3 | SHOULD | The dirty-target half of `test_revendor_refuses_symlinks_and_a_dirty_target` is vacuous: it passes because the tool cannot assess cleanliness from the pytest CWD, not because the file is dirty. `tests/test_archify_vendor.py:369-397` | F2's clean-target repro exits 2 with the same assertion satisfied; no test asserts the tool's `uncommitted changes` message (`grep -rn "uncommitted" tests/` → only the fixture docstring at `:370`) | Invoke that leg with `cwd=tmproot` (or assert `"uncommitted changes" in _out(proc)`) so the clean/dirty distinction is actually exercised |
| F4 | SHOULD | `docs/scripts.md:36`'s maintainer re-vendor command is incomplete (no `--repo`, abbreviated sha, no `--commit`); following it erases the commit pin and turns a committed test red. `VENDOR.md:28-31` carries the correct full command | Re-vendor the genuine zip *without* `--commit` → regenerated `vendor.json` has `"commit": ""`; `tests/test_archify_vendor.py:182` asserts `upstream["commit"] == EXPECTED_COMMIT` (`c826e6c3…`). `docs/scripts.md:36` shows `gh release download v2.16.0 -p archify.zip` and `--expect-sha256 4c59fa65…` with no `--repo`/`--commit` | Point the row at `VENDOR.md`'s exact command (or paste `--repo tt-a1i/archify`, the full sha, `--commit c826e6c3…`) |
| F5 | SHOULD | Plan PKG-4/QA-6 required a SHA-pinned `actions/setup-node` (node 20) on the pytest job; no workflow adds it, so the node legs run on the runner image's unpinned node | `grep -rn "setup-node" .github/workflows` → no output; `.github/workflows/pylint.yml:19-46` (tests job) sets up only Python. The legs fail rather than skip when `CI` is set, so a node-less runner goes red (the safety net exists) | Add the pinned `actions/setup-node` step with `node-version: "20"` before `verify.sh pytest` |
| F6 | NOTE | `stage_zip` rejects `/`-separated `..` and leading `/` only: backslash and drive-letter entry names pass validation, and on Windows `staging / rel` escapes. `tooling/vendor_archify.py:314-316` | Probe importing the module: `archify/..\..\evil.txt` and `archify/C:/win/evil.txt` are ACCEPTED; `ntpath.normpath(ntpath.join('C:\\stage', '..\\..\\evil.txt')) == 'C:\\evil.txt'`. On POSIX both land as literal filenames inside staging. `docs/getting-started.md:36` lists Windows as out of scope, so this is defense-in-depth | Reject `\` and `:` in entry names, or resolve the destination and require containment under `staging` |
| F7 | NOTE | The adapted Gotcha "omitting or misspelling it silently drops out of showcase validation" misstates the tool: misspelling is a loud schema failure, and omission is overridden by the `--quality showcase` the same skill instructs. `features/common/skills/archify/SKILL.md:168` | Misspelled `meta.quality_profile` → `validate … rc=1`, `ok:false`, `schema/enum` error; omitted + `--quality showcase` → `ok:true`, `compositionProfile=showcase`, 9 checks; omitted without the flag → `profile=standard`, `ok:true`. (The adjacent "4 artifact checks" line at `:170` repeats upstream v2.16.0 body text; every profile I ran produced 9 checks, but I did not falsify the conditional) | Reword: "must be `standard` or `showcase`; a misspelling fails schema validation, and omission falls back to `standard` unless `--quality` overrides" |
| F8 | NOTE | The offline `--check` is manifest-relative by design: modified bytes plus a rewritten `vendor.json` hash pass. `tooling/vendor_archify.py:245-283` | Flipped a byte in `renderers/architecture/grid.mjs`, rewrote its hash in `vendor.json` → `--check` `rc=0`. Boundary stated honestly in `VENDOR.md:20-24` and `docs/scripts.md:36`; `run_check` never reads `asset_sha256` (only `build_manifest` writes it, `:372,:415`) | None; recorded so `--check` is not read as an origin check. The origin anchor remains `--revendor --expect-sha256`, whose literal is operator-supplied with no in-tool constant |
| F9 | NOTE | With `git` absent, `target_is_dirty` raises an uncaught `FileNotFoundError` (rc 1 traceback) instead of the documented exit 2. `tooling/vendor_archify.py:349` | `env PATH=/nonexistent … vendor_archify.py --revendor … --root /tmp/aib_exdev/nogit` → `FileNotFoundError: [Errno 2] No such file or directory: 'git'`, process rc 1; the tool contract says exit 2 on any failure (tree untouched) | Wrap the `subprocess.run` calls in `target_is_dirty` and raise `VendorError` on `OSError` |
| F10 | NOTE | `run_check`'s `EXCLUDED` branch has no red witness: no test asserts the tool ever emits it, so the loop can be deleted without a test going red. `tooling/vendor_archify.py:279-282`; `tests/test_archify_vendor.py:196-203` | `grep -rn "EXCLUDED" tests/` → no assertion on the tool (only an unrelated `conftest` constant); the every-check provocation plants a hash change and expects `CHANGED` (`tests/test_every_check_can_fail.py:918`). The clean-tree test calls `bl.excluded_by_patterns` directly and stays green without the loop | Add a planted `test_probe.py` (present on disk and in the manifest) asserting `--check` → `EXCLUDED` + exit 1 |
| F11 | NOTE | ADR-0030:31 says "byte-identical for the 76 upstream files"; the precise fact is 75 byte-identical plus the adapted `SKILL.md` (as `vendor.json`/`VENDOR.md:46` state). Also, a re-vendor rewrites `vendor.json`'s `staged_by` to the tool's canned text — a cosmetic diff nothing hashes | Genuine zip: 76 files, `cmp` → 75 identical, only `SKILL.md` differs; zipped body sha == `vendor.json.adapted.SKILL.md.upstream_body_sha256`. Documented re-vendor then `diff -r` vs committed → only `vendor.json` `staged_by` | ADR wording: "75 byte-identical upstream files plus the adapted `SKILL.md`"; accept the `staged_by` diff or have `build_manifest` preserve the existing value |

## Verified holds

1. **Origin anchor (QA-3).** The genuine `archify.zip` sha `4c59fa6557a2…` equals the pinned literal; `--revendor --expect-sha256 4c59fa65… --commit c826e6c3…` on a scratch copy → rc 0, and `diff -r` against the committed tree is byte-identical except the informational `staged_by`. The zipped `SKILL.md` body hashes to `vendor.json`'s `upstream_body_sha256`; 75 of 76 files are `cmp`-identical. `asset_sha256` is written by `build_manifest` only and never read by `run_check`. The forged-packet test (forged zip + forged manifest that agree + the true CLI literal) exits 2 with the tree untouched.
2. **Adaptation reversal.** Stripping the appendix and reversing the line-82 fix reconstructs the pristine upstream body (proven against the real zip); body and frontmatter tampers both report `CHANGED SKILL.md`.
3. **`system` dependency.** `tests/test_dependency_check.py` + `tests/test_archify_integration.py`: 40 passed. Present → `already_present`, zero subprocess calls in both modes; absent + no command → one hint naming Node/18/Mermaid; `--execute` + declared command → `shlex.split` argv with `shell=False`; shipped archify entry declares no `command`; `needs_venv` stays python-only.
4. **Adapted SKILL claims.** `ARCHIFY_UPDATE_CHECK_DISABLED=1` is honored (`scripts/check-update.mjs:1637`); `visual-check` exit 0/1/2 = pass/overflow/capture-fail vs Chrome-absent `skipped` (`bin/visual-check.mjs:24,733,804,824`); a failed `deliver` preserves the artifact (`bin/archify.mjs:926`); a showcase receipt reports 9/9 checks, 0 errors, 0 warnings.
5. **Gates (all green).** `vendor_archify.py --check` rc 0 (76 files); `pytest` focused: 16 + 40 + 2 + 41 passed; `node --test tests/js/archify_packet.test.mjs` 3/3; `gates/skills_lint.py` rc 0 (54 files); `validate.py --all` rc 0; `version_sync.py --check`, `index_build.py --check`, `sync_plugin_skills.py --check` rc 0; `scaffold_freshness_guard.py` PASS; `pylint` 10.00/10 on both authored Python files.
6. **Fallback consistency.** The Mermaid fallback reads the same way in the invariant, the frontmatter description, the SKILL body, ADR-0030 and both documentation members; the invariant passes the 6-line cap and its title+link land in the scaffolded agent file.

## Verdict

**fold-then-merge.** No MUST finding. Every plan-critical claim verifies against the genuine upstream asset: the origin sha is not self-referential, a forged packet is refused, the adaptation reversal reconstructs the pristine body, `system` presence never installs without consent plus a declared command, and the scaffolded delivery matches the catalog byte for byte. The five SHOULDs are localized and cheap (two are one-line `dir`/`cwd` changes, one test assertion, one docs command, one missing CI step); fold F1-F5 before merge, and F6-F11 can ride along in the same pass.
