# work — dated records of work done, not documentation of how things are

A work record describes a moment: what was proposed, what was decided, what was reviewed, what
broke. It is true of its date and is never updated to stay current — that is what makes it a
record. Documentation of how the framework works today lives in the quadrants
(`../reference/`, `../explanation/`, `../how-to/`, `../tutorials/`); decisions that bind live in
`../adr/`.

Filenames are `YYYY-MM-DD-slug`. A file here without a date is a placement error.

This directory is deliberately *not* grouped into `plans/`, `research/`, `designs/`, `reviews/`
or `incidents/`. Kind is not a subject, and those five were tried and removed — see
`../adr/` and PR #111.

## Files

| File | What it records |
|---|---|
| `2026-08-01-post-backlog-review.html` | Owner review gate over the seven refinement decisions raised while clearing the backlog (P1, P2, W1, F1, F2, B1, B2). Generated by the `owner-gate-review` skill; the form carries each decision's claim and detail verbatim. |
| `2026-08-01-session-decisions-review.html` | Owner review gate over the six decisions raised during the 0.61–0.66 release run (D1–D6). D6 is the one that produced this directory. |
| `2026-08-01-gherkin-spec-elicitation-research.html` | Research validating a `create-task-spec` skill that elicits a Gherkin specification by questioning rather than proposing. Records six claim verdicts, the grammar-as-question-generator argument, and the architecture chosen on 2026-08-01. |
| `2026-08-01-work-quality-rules.md` | Where each of sixteen proposed working rules belongs — five become `common` invariants, the rest are task lifecycle, delegation policy, or the `--risk` switch. Written while four packages were running against it; leaves once they merge. |
| `2026-08-02-hermes-task-tracking.md` | Evidence-first research on how the `/task` skill's tracking is (and is not) hooked into Hermes Agent: the four task hooks are Claude-only by design, the scaffolded scripts never fire under Hermes, every tracked task's checkpoints are all-zero — and Hermes's `state.db` demonstrably stores the per-model and per-subagent token data the tracker never reads. 14 findings (10 measured, 4 read). |
| `2026-08-05-ai-raccoon-memory-quality.jsonl` | The session's manual memory-search quality log: two graded `memory_search` calls from the ai-raccoon integration work, recorded as the reference shape the memory-grade hook's JSONL lines are verified against. |
| `2026-08-05-mcp-hermes-setup-flow.md` | Research record (task mcp-hermes): how Hermes actually discovers and loads MCP servers — `mcp_servers` from `~/.hermes/config.yaml` only, healthy load path, broken `hermes mcp list --json` reporting (issue #188) — with actual vs expected mermaid graphs, a diff table, and the upstream fix (NousResearch/hermes-agent#79690). |
| `2026-08-06-hermes-integration-diagnosis.md` | Root-cause diagnosis of the dead Hermes-side hook integration: ai-badger deploys flat `.py` files into `~/.hermes/plugins/`, but Hermes only loads directory plugins (`plugin.yaml` + `register(ctx)`, opt-in via `plugins.enabled`), so none of the four hermes entries in `hooks-manifest.json` has ever fired. Subsumes the 2026-08-02 task-tracking findings (F3's "the plugin registers" premise was a reading of file contents, not runtime). |
| `2026-08-06-memory-skill-design-review.md` | Design review of the ai-raccoon memory skill and its memory-grade hook (structure, purpose fit, hook design, failure-mode coverage), with concrete improvement opportunities — produced by a review subagent. |
| `2026-08-06-hermes-plugin-fix-review.md` | Code-reviewer gate for PR #311 (the Hermes directory-plugin fix): verdict APPROVE-WITH-NITS, 7 findings (F-01 extension.md re-creates the bug, F-02 Copilot host attribution, F-03/F-04 stale docs, F-05 debug_log not shipped, F-06 string args, F-07 RED verified 22 failed on main), A–E checklist, merge conditions. |
| `2026-08-07-skills-optimization-audit.md` | Measured audit (task opt-skills) of the 23 distributed SKILL.md files against agentskills.io best practices: descriptions/size/procedures already strong; general gaps G1-G6 (gotchas convention, progressive-disclosure conditions, frontmatter metadata, skills-lint gate, verification checklists) and individual items I1-I14; G5 premise corrected against source (root skills/ shapes are intentional, gate-enforced). |
| `2026-08-07-opt-skills-plan.md` | Integrated executable plan (task opt-skills) from 3 parallel planning agents: PR-1 corpus conventions (G1/G2/G3/G6), PR-2 individual edits (I1-I12), PR-3 skills-lint in validate.py, PR-4 shape assertion — each item with concrete acceptance greps and gate-wired sync steps. Review record pending the code-reviewer MoE round. |
| `2026-08-07-full-project-review-charter.md` | What the seven-expert full-project review had to check, written before any expert was dispatched: the corrected premise (2 `.cs` files, so `features/dotnet` is consumer-facing guidance rather than architecture this repo uses), the week's measured scope (93 commits, 646 files), the panel's seven lenses, and the standing evidence rules each expert worked under. |
| `2026-08-07-full-project-review-findings.md` | Live findings checkpoint for the same review: each expert's report folded in as it landed, plus the findings the orchestrator established directly — the empty ai-raccoon bank, the cwd-dependent task tracker, the stuck tracker entries — and the integrated plan built from all of them. |
| `2026-08-07-full-project-review-plan.md` | The integrated plan the seven reports were folded into: six themes (gates that do not gate, automation that damages or hides, harvest leakage, architecture, docs truth), ten work items chunked for dispatch, a three-wave parallel schedule sized to the measured 20-slot agent pool, and the nine decisions only the owner can make. |
| `2026-08-12-semantica-integration-review.md` | Evidence review for the semantica-integration task: what Semantica is (MIT, v0.6.5, graph-native KG), its 12 MCP tools, live dogfooding results (state accumulation confirmed, `get_graph_analytics` broken in 0.6.5, extraction degraded without torch/transformers, server is in-memory per process), licensing check (MIT — same terms as code-review-graph), and complementarity with AiRaccoon memory. |
| `2026-08-12-semantica-integration-plan.md` | Implementation plan (501 lines) from the architect agent: catalog entry (meta.json, server.md, tools.json — 11 tools), stack-mcp.json wiring, integration skill SKILL.md (4 workflows, escalation guide, AiRaccoon complementarity), 22 TDD tests, dogfooding plan, and risk register. |
| `2026-08-12-semantica-integration-plan-review-architect.md` | Architecture review of the integration plan: 9 findings (2 CRITICAL — broken `get_graph_analytics`, no import for exported graphs; 2 HIGH — state accumulation unverified, extraction degradation misattributed; 3 MEDIUM; 2 LOW). Verdict: APPROVE-WITH-FIXES. |
| `2026-08-12-semantica-integration-plan-review-test.md` | Test-engineer review of the integration plan: 18 findings (5 HIGH — RED-GREEN cycle unenforceable, no sensitivity tests, missing `validate.py --all` in gate chain, evidence findings untested, dogfooding Scenario 2 impossible). Verdict: APPROVE-WITH-FIXES. |
| `2026-08-12-semantica-integration-moe-synthesis.md` | Synthesis of both MoE reviews into 19 combined findings with remediation actions and an adjusted execution order (Scenario 0 first, Lane C RED before A+B, revised dogfooding scenarios). |
| `2026-08-12-semantica-integration-part2-extraction-and-lifecycle.md` | Research and analysis record for semantica-integration-part2: evaluation of local extraction options (Option 2 Agent-Guided recommended vs Option 1 torch/transformers), process isolation mechanics (100% in-memory per stdio process), "no import" lifecycle consequences (exports are archival only), and complementarity rules vs AiRaccoon. |
| `2026-08-12-semantica-export-hook-and-watch-plan.md` | Implementation plan for Semantica export hook, seeded graph template (`.ai-raccoon/semantica-graph.json`), and AiRaccoon watch bridge. |
| `2026-08-12-semantica-export-hook-and-watch-plan-review-architect.md` | Architecture review of the export hook plan (architect subagent): 3 findings (tempfile process-isolation, soft exit 0, template validation), verdict APPROVE-WITH-FIXES. |
| `2026-08-12-semantica-export-hook-and-watch-plan-review-test.md` | Test engineer review of the export hook plan (test-engineer subagent): 3 findings (hyphenated import spec, sensitivity tests, gate chain), verdict APPROVE-WITH-FIXES. |
| `2026-08-12-semantica-export-hook-and-watch-moe-synthesis.md` | MoE synthesis record for the export hook and watch bridge plan: consensus APPROVED with all 5 findings remediated and verified. |
| `2026-08-18-semantica-session-export-diagnosis.md` | Diagnosis (task semantica-session-export): Semantica is integrated but dormant — four gaps (no lifecycle hook exports the graph, no guidance nudge, single-file `.ai-raccoon/semantica-graph.json` overrides sessions, manual file-scoped watch), plus the hard constraints that shape the fix (graph is only reachable via the agent's `export_graph` result; AiRaccoon's directory watch ingests new files). |
| `2026-08-18-semantica-session-export-plan.md` | Implementation plan for #414–#417 synthesized from 2 parallel MoE planning lanes: per-session `.semantica/<session>-<ts>.json` target, auto-save hook reusing `export_semantica_graph.py` as a lazy sibling (skip-on-error), index-gated once-per-session nudge, skill-only directory watch, and a 4-unit worktree/PR decomposition. |
| `2026-08-18-semantica-session-export-moe-synthesis.md` | MoE review of the plan (code-reviewer + test-engineer, both APPROVE-WITH-FIXES) with 12 folded findings: the Hermes MCP result is a double-encoded `{"result":…}` envelope (extract_graph_json contract corrected), the "watch is creation-triggered" premise was false (timestamp kept for session-collision, not overwrite-miss), `.tmp.<pid>` must land outside `.semantica/`, a real `semantica-export-autosave` manifest entry with claude/copilot exemptions, and the test-surface corrections (shipping-list parity, ai-raccoon-memory SKILL test, sync gate, usage-gate membership, time_ns slug). |
| `2026-08-20-semantica-integration-fix-plan.md` | Plan (task semantica-integration-fix): U0 upstream semantica export_graph bug fix (json branch TypeError + RDF progress-bar stdout corruption — both live in 0.6.5/0.6.6/main), U1 reject error payloads inside the export envelope (proven live), U2 wire claude/copilot autosave + nudge (#418), U3 functional export probe in check.py + default-distribution pin, U4 docs/changelog, U5 den-refresh + watch + user-level update. |
| `2026-08-23-semantica-nudge-claude-copilot-plan.md` | Implementation plan for #418 remaining half: port Semantica once-per-session export guidance nudge to Claude Code and GitHub Copilot via context_enrichment_hook.py marker state. |
| `2026-08-23-research-based-prompt-refactor-plan.md` | Research-based refactor plan: synthesized 4 prompt-engineering research documents, audited personas/invariants/templates against evidence, identified 10 gaps and 10 prioritized changes. |
| `2026-08-28-aib-pi-review-fixes-moe-python.md` | MoE plan lane (rev-1 snapshot, task aib-pi-review-fixes): the Python-adjustment half — findings F1/F3/F4/F8/F9/F10 plus the F3/F7 doc halves in the pi task extension, each anchored to facts measured first-hand against pi 0.84.3 in the worktree. The master plan wins where the two disagree. |
| `2026-08-28-pi-full-support-gaps.md` | Gap audit (§7 of the master plan, task aib-pi-review-fixes): for every feature the owner uses daily, pi gets at least a simple usable form; full parity is out of scope and every deferral carries a one-line justification. Replacement blockers are listed first. |
| `2026-08-28-pi-harness-support.md` | Evidence-graded research record (task add-support-for-pi-harness): 24 findings on delivering ai-badger through the pi harness — a pi extension runs the existing Python hooks unmodified because they dispatch on dialect not harness identity (12/16 hooks day one); pi has no MCP by design, so five servers stop existing; headless pi at default trust silently ignores project-local extensions while still loading CLAUDE.md (demonstrated over 7 runs with a control), mitigated by user-scope install. All four providers built in. A separate repo cannot register a harness. Recommends swapping hermes for pi rather than adding a fourth surface. |
| `2026-08-28-pi-migration-plan.md` | Implementation plan for pi as a fourth ai-badger agent (task add-support-for-pi-harness): phase 0 trust-sentinel probe (`tooling/test_pi_hook_fires.py`), then MCP bridge via a forked pi-mcp-tools extension, harness detection, adjust_\* arms, cron, and task-skill extension — with effort estimates and an open-questions list. |
| `2026-08-28-pi-review-fixes-fork-plan.md` | Plan (lane 4 of 4, task aib-pi-review-fixes): the pi-mcp-tools fork work for review findings 11–18 — one branch, one PR on `Arasz/pi-mcp-tools`. Baseline re-verified in the fork repo, not taken from the review; contains no code. |
| `2026-08-28-pi-review-fixes-moe-ts.md` | Architect lane plan (task aib-pi-review-fixes): the TypeScript / pi-extension deliverables — hooks adapter (F1), cron extension (F2), `no_agent` default (F5), extension.md docs (F7 + F3 doc half), and the away-mode scope item. Python-side fixes belong to other lanes. |
| `2026-08-28-pi-review-fixes-plan.md` | Master plan rev 2 (task aib-pi-review-fixes): fix all 18 review findings plus a simple, usable pi form of every daily feature; owner bar is pi replacing hermes as the daily harness, so the integration package carries a machine-cutover gate. Carries the MoE review disposition (2 MUST, 9 SHOULD/NIT) with each finding mapped to its plan section. |
| `2026-08-28-pi-review-fixes-test-strategy.md` | Test-engineer lane (task aib-pi-review-fixes): what proves the fixes — anchored on F6 (install path untested let two blockers go invisible); owns the tests, not the production changes, which the implementing lanes deliver. |
| `2026-08-28-pi-review-fixes-plan.md` | Plan rev 2 (task aib-pi-review-fixes): fixes all 18 review-findings for the pi agent and closes the four replacement blockers (hooks, skills, MCP, unattended) with simple usable forms — 18 decisions with MoE-review disposition mapping, gap packages G1–G5, work packages for ai-badger + the pi-mcp-tools fork, and a machine-cutover checklist verifying the daily loop live in pi (owner bar: pi replaces hermes; parity not required). |
| `2026-08-28-semantica-support-research.md` | Evidence record (task continue-semantica-support-fixes): measured on semantica 0.6.6 — the user-scope install destroys its own uv tool env on Python 3.14 (gensim has no cp314 wheel), every `export_graph` format errors, the RDF branch writes a progress bar to stdout that corrupts the MCP transport, and `SEMANTICA_DISABLE_PROGRESS=1` converts that into a clean error response. Records that the 0.137.1 wrapper cannot reach a consumer launch path. |
| `2026-08-28-semantica-support-plan.md` | Implementation plan built on that record: nine points with acceptance criteria, gates and failing-first tests, plus the limits stated rather than papered over (the `env` gate is structural only, the wheel ceiling is inference, the docs point has no gate that can tell a true claim from a false one). |


The first three are generated HTML forms rather than prose. They are kept because the form *is* the
record: each carries its decisions verbatim, and the reviewer's verdicts were ingested from them.
The third is a research record — true of its date, superseded rather than updated. The fourth is
the first plain-markdown record here.
