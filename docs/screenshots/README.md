# screenshots — captured images used by the pages in this tree

Predates `../assets/`, which is where images belong under the canonical tree. It stays here
because `owner-gate-review.png` is linked from `../skills.md` and named in `.ai-badger/state.json`
— freeze-list rule 2 makes re-placing a pinned path its own PR, never part of a migration.

Screenshots are frozen against reformatting by rule 6 (not `.md`).

## Files

| File | What it shows |
|---|---|
| `owner-gate-review.png` | A generated `owner-gate-review` form: fourteen decision cards, each with an id, a summary, an APPROVE / CHANGE / REJECT / DEFER row and a notes box, over a sticky answered-count footer |
