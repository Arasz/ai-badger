{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Arasz/ai-badger/schemas/marketplaces.schema.json",
  "title": "ai-badger custom marketplaces",
  "description": "Declares the custom Claude Code marketplaces a plugin entry installs from. Kept beside plugins.json in each <stack>/plugins/<name>/ dir.",
  "type": "object",
  "required": ["marketplaces"],
  "additionalProperties": false,
  "properties": {
    "marketplaces": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["name", "source"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "source": {
            "type": "string",
            "description": "Marketplace source: 'github:Owner/repo', a git URL, or a local path — as accepted by `claude plugin marketplace add`."
          }
        }
      }
    }
  }
}
